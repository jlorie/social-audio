{
  "registerRequest": {
    "application/json": "{ \"action\": \"register\", \"username\": $input.json('$.username'), \"fullname\": $input.json('$.fullname'), \"genre\": $input.json('$.genre'), \"birthdate\": $input.json('$.birthdate'),\"password\": $input.json('$.password')}"
  }
}
